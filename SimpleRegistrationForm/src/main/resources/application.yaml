server:
  port: 8090

spring:
  application:
    name: SimpleRegistrationForm

  datasource:
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
